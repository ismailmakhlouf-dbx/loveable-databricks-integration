"""
FastAPI dependencies for {{ project_name }}.

Auto-generated by Lovable Bridge MCP Server.
"""

from fastapi import Depends, HTTPException, status
from fastapi.security import HTTPBearer, HTTPAuthorizationCredentials
from databricks.sdk import WorkspaceClient
import os

security = HTTPBearer()

def get_current_user_id(
    credentials: HTTPAuthorizationCredentials = Depends(security)
) -> str:
    """
    Extract and validate user ID from Databricks OAuth token.

    Args:
        credentials: HTTP Bearer token credentials

    Returns:
        User ID string

    Raises:
        HTTPException: If token is invalid
    """
    try:
        # Validate token with Databricks OAuth
        workspace = WorkspaceClient()
        # TODO: Implement actual token validation
        # For now, extract from token (placeholder)
        token = credentials.credentials

        # This is a placeholder - implement actual validation
        if not token:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Invalid authentication credentials",
                headers={"WWW-Authenticate": "Bearer"},
            )

        # Extract user ID from token
        # TODO: Implement proper token decoding
        user_id = "user_from_token"

        return user_id

    except Exception as e:
        raise HTTPException(
            status_code=status.HTTP_401_UNAUTHORIZED,
            detail=f"Could not validate credentials: {str(e)}",
            headers={"WWW-Authenticate": "Bearer"},
        )

def get_workspace_client() -> WorkspaceClient:
    """
    Get Databricks workspace client.

    Returns:
        WorkspaceClient instance
    """
    return WorkspaceClient()
